'use strict'

const {
  createQueryStore,
  addQueryParams,
  removeQueryParams
} = require('./build/querystring-cache.js')

module.exports = {
  createQueryStore,
  addQueryParams,
  removeQueryParams
}
