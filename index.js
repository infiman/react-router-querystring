'use strict'

const {
  QUERYSTRING_CACHE_STATE_KEY,
  createQueryStore,
  addQueryParams,
  removeQueryParams
} = require('./build/querystring-cache.js')

module.exports = {
  QUERYSTRING_CACHE_STATE_KEY,
  createQueryStore,
  addQueryParams,
  removeQueryParams
}
